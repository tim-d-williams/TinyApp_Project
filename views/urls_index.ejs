<!DOCTYPE html>
<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
  <% include ./partials/_header %>
</head>
<body>
  <ul>
    <% for(var item in urls) { %>
      <li><%= urls[item] %></li>
      <% } %>
  </ul>
  <p><a href='/urls/new'> Submit URL for shortening</a>
</body>
</html>
